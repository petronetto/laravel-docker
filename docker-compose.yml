version: '2'
services:
  php:
    container_name: php
    image: petronetto/php-nginx:laravel
    volumes:
     - ./application:/var/www/html
    links:
     - database
     - redis
    ports:
     - "80:80"
     - "443:443"
  database:
    container_name: database
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: homestead
      MYSQL_USER: homestead
      MYSQL_PASSWORD: secret
    volumes:
     - ./data:/var/lib/mysql
    ports:
     - "3306:3306"
  redis:
    container_name: redis
    image: redis:alpine
volumes:
  data:
    driver: "local"
